interface:
  display_name: "Version Release"
  short_description: "Automate commit, push, tagging, and release workflows"
  default_prompt: "Use $version-release to commit pending changes, push, and publish the next version tag."
